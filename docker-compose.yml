services:
  mysql:
    image: mysql:8.0
    container_name: rpg_mysql
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: rpgconquestgame
      MYSQL_USER: rpg_user
      MYSQL_PASSWORD: rpg_pass
      GAME_PREFIX: game2_
    ports:
      - "3307:3306"
    volumes:
      - rpg_mysql_data:/var/lib/mysql
      - ./docker/init_mysql.sh:/docker-entrypoint-initdb.d/init_mysql.sh
      - ./var/mysql/setupBDD.sql:/tmp/setupBDD.sql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "root", "-proot"]
      interval: 5s
      timeout: 5s
      retries: 10

  php:
    build: .
    container_name: rpg_php
    ports:
      - "8080:80"
    volumes:
      # Game 2 (default) — URL: /RPGConquestGame, prefix: game2_
      - .:/var/www/html/RPGConquestGame
      # Game 1 — URL: /RPGConquestGame1, prefix: game1_ (same source, different config)
      - .:/var/www/html/RPGConquestGame1
      - ./docker/local_config_game1.ini:/var/www/html/RPGConquestGame1/var/local_config.ini:ro
    environment:
      DB_HOST: mysql
      DB_NAME: rpgconquestgame
      DB_USER: rpg_user
      DB_PASS: rpg_pass
      DB_TYPE: mysql
      FOLDER: RPGConquestGame
      GAME_PREFIX: game2_
    depends_on:
      mysql:
        condition: service_healthy

volumes:
  rpg_mysql_data:
